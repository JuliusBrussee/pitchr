# Rubric Matrix Runner Registry

Auto-generated by `yarn sync:rubric-matrix-runners`.

Use these scripts to run Anthropic-only rubric influence matrix tests per project type.

| Project Type | Workflow Mode | Terminal Command | Generated Scripts |
|---|---|---|---|
| `two_min_pitch` (Two-Minute Pitch) | `vc_pitch` | `yarn rubric:matrix:anthropic --mode vc_pitch --out-dir ".cache/rubric-sandbox/matrix-anthropic-two_min_pitch"` | [ps1](../scripts/generated/rubric-matrix/run-two_min_pitch.ps1), [cmd](../scripts/generated/rubric-matrix/run-two_min_pitch.cmd), [sh](../scripts/generated/rubric-matrix/run-two_min_pitch.sh) |
| `elevator_pitch` (Elevator Pitch) | `elevator` | `yarn rubric:matrix:anthropic --mode elevator --out-dir ".cache/rubric-sandbox/matrix-anthropic-elevator_pitch"` | [ps1](../scripts/generated/rubric-matrix/run-elevator_pitch.ps1), [cmd](../scripts/generated/rubric-matrix/run-elevator_pitch.cmd), [sh](../scripts/generated/rubric-matrix/run-elevator_pitch.sh) |

## Notes

- Add new project types in `config/projectTypes.ts`.
- Re-run `yarn sync:rubric-matrix-runners` (also runs automatically in `predev` and `prebuild`).
- Each runner writes results to its own out-dir under `.cache/rubric-sandbox/`.
