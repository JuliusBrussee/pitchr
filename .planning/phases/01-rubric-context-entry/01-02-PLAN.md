---
phase: 01-rubric-context-entry
plan: "02"
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - app/(app)/projects/page.tsx
  - tests/projects-rubric-context.test.tsx
autonomous: true
requirements:
  - PRJC-01
  - PRJC-02
  - VAL-01
must_haves:
  truths:
    - Users can find a dedicated `Rubric & Context` section inside each project card.
    - Users can save rubric/context only when the trimmed value is non-empty and <= 4,000 chars.
    - Invalid input shows inline errors and never triggers persistence.
    - Save UX shows unsaved state, success feedback, and retry-on-failure while preserving draft text.
  artifacts:
    - path: app/(app)/projects/page.tsx
      provides: Card-scoped rubric/context editor UI with manual save flow.
      contains: Edit Rubric & Context
    - path: tests/projects-rubric-context.test.tsx
      provides: Integration coverage for section visibility, validation, and save interaction.
      contains: describe('ProjectsPage rubric context')
  key_links:
    - from: app/(app)/projects/page.tsx
      to: views/components/ProjectProvider.tsx
      via: `updateProject` call with merged `promptOverrides`.
      pattern: promptOverrides.*analysis_system_prompt
    - from: app/(app)/projects/page.tsx
      to: supabase/functions/_shared/rubric-context.ts
      via: shared max-length and validation helpers used for client gating.
      pattern: RUBRIC_CONTEXT_MAX_CHARS|validateRubricContextForSave
    - from: tests/projects-rubric-context.test.tsx
      to: app/(app)/projects/page.tsx
      via: render + interaction assertions for CTA, counter, and save workflow.
      pattern: Edit Rubric & Context|Unsaved changes|Saved just now
---

<objective>
Implement the `/projects` card-level Rubric & Context editor and manual save UX using locked decisions.

Purpose: Deliver PRJC-01 and PRJC-02 with VAL-01 user-facing validation behavior.
Output: Projects-page editor interaction plus automated integration tests.
</objective>

<execution_context>
@C:/Users/20243455/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/20243455/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-rubric-context-entry/01-CONTEXT.md
@.planning/phases/01-rubric-context-entry/01-RESEARCH.md
@app/(app)/projects/page.tsx
@views/components/ProjectProvider.tsx
@types/project.ts
@supabase/functions/_shared/rubric-context.ts

<interfaces>
From `views/components/ProjectProvider.tsx`:
```typescript
updateProject: (input: {
  projectId: string;
  name?: string;
  isArchived?: boolean;
  promptOverrides?: ProjectPromptOverrides;
  setActive?: boolean;
}) => Promise<void>;
```

From `types/project.ts`:
```typescript
export interface ProjectPromptOverrides {
  analysis_system_prompt?: string;
  [key: string]: unknown;
}
```

From `app/(app)/projects/page.tsx`:
```typescript
const { projects, activeProjectId, updateProject } = useProject();
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Rubric &amp; Context editor surface and validation UX in project cards</name>
  <files>app/(app)/projects/page.tsx, tests/projects-rubric-context.test.tsx</files>
  <behavior>
    - Test 1: Each project card renders a dedicated `Rubric & Context` section with collapsed `Edit Rubric & Context` CTA.
    - Test 2: Only one project editor can be open at a time.
    - Test 3: Mobile layout presents a focused full-screen editor view when opened.
    - Test 4: Textarea shows live `current/max` counter and inline validation errors for empty/oversized values.
  </behavior>
  <action>Implement card-scoped editor state in `ProjectsPage` with `openProjectId` + per-project drafts. Import and use the shared rubric/context validation contract from Wave 1 (`RUBRIC_CONTEXT_MAX_CHARS` + validator). Enforce hard typing limit via textarea `maxLength`, show inline red error state, and keep the editor collapsed by default with the locked CTA copy. Do not introduce autosave.</action>
  <verify>
    <automated>yarn test -- tests/projects-rubric-context.test.tsx</automated>
  </verify>
  <done>Rubric/context section is discoverable in project cards, supports single-open editing, and enforces client validation UX rules from locked decisions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement manual save workflow, dirty-state feedback, and retry behavior</name>
  <files>app/(app)/projects/page.tsx, tests/projects-rubric-context.test.tsx</files>
  <behavior>
    - Test 1: Save stays disabled until trimmed input is valid.
    - Test 2: `Unsaved changes` appears whenever draft differs from saved value.
    - Test 3: Successful save shows inline success (`Saved just now`) and clears dirty state.
    - Test 4: Failed save keeps draft text, shows inline error, and exposes a `Retry` action.
    - Test 5: Save payload merges existing `promptOverrides` and only updates `analysis_system_prompt`.
  </behavior>
  <action>Wire the Save button to `updateProject` with payload merge (`{ ...project.promptOverrides, analysis_system_prompt: normalizedValue }`) to prevent override clobbering. Keep save explicit/manual per locked decision. On failure, keep draft intact and keep editor open; on success, update local saved baseline and show inline success timestamp text. Ensure invalid input never calls `updateProject`.</action>
  <verify>
    <automated>yarn test -- tests/projects-rubric-context.test.tsx</automated>
  </verify>
  <done>Users can manually save valid rubric/context text with clear unsaved/success/failure states, and invalid input is blocked from persistence calls.</done>
</task>

</tasks>

<verification>
- `yarn test -- tests/projects-rubric-context.test.tsx` passes.
- Save payload preserves existing `promptOverrides` keys while updating `analysis_system_prompt`.
</verification>

<success_criteria>
- PRJC-01: Dedicated Rubric & Context section exists in project card UX.
- PRJC-02: Valid rubric/context input persists through project update path.
- VAL-01: Invalid input shows inline error and is not saved.
</success_criteria>

<output>
After completion, create `.planning/phases/01-rubric-context-entry/01-rubric-context-entry-02-SUMMARY.md`
</output>
