---
phase: 01-rubric-context-entry
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/_shared/rubric-context.ts
  - supabase/functions/projects/index.ts
  - tests/rubric-context-validation.test.ts
autonomous: true
requirements:
  - VAL-01
must_haves:
  truths:
    - Invalid rubric/context input (trimmed-empty or over 4,000 chars) cannot be persisted.
    - Valid rubric/context input is normalized before persistence.
  artifacts:
    - path: supabase/functions/_shared/rubric-context.ts
      provides: Shared rubric/context validation contract usable by UI and edge handlers.
      exports:
        - RUBRIC_CONTEXT_MAX_CHARS
        - getRubricContextCharacterCount
        - validateRubricContextForSave
    - path: supabase/functions/projects/index.ts
      provides: POST/PATCH handlers enforce rubric/context validation before create/update.
      contains: ProjectValidationError
    - path: tests/rubric-context-validation.test.ts
      provides: Automated regression coverage for trim/empty/max-length rules.
      contains: describe('rubric context validation')
  key_links:
    - from: supabase/functions/projects/index.ts
      to: supabase/functions/_shared/rubric-context.ts
      via: Shared validator import in POST/PATCH request handling.
      pattern: validateRubricContextForSave
    - from: tests/rubric-context-validation.test.ts
      to: supabase/functions/_shared/rubric-context.ts
      via: Direct unit tests of exported contract.
      pattern: RUBRIC_CONTEXT_MAX_CHARS|validateRubricContextForSave
---

<objective>
Establish a single validation contract for rubric/context input and enforce it in the projects edge function.

Purpose: Guarantee VAL-01 at the system boundary, not only in client UX.
Output: Shared validator module, edge-handler integration, and automated regression tests.
</objective>

<execution_context>
@C:/Users/20243455/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/20243455/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-rubric-context-entry/01-CONTEXT.md
@.planning/phases/01-rubric-context-entry/01-RESEARCH.md
@supabase/functions/projects/index.ts
@supabase/functions/_shared/types.ts

<interfaces>
From `supabase/functions/_shared/types.ts`:
```typescript
export interface ProjectPromptOverrides {
  analysis_system_prompt?: string;
  [key: string]: unknown;
}
```

From `supabase/functions/projects/index.ts`:
```typescript
class ProjectValidationError extends Error {}

promptOverrides:
  body.promptOverrides && typeof body.promptOverrides === 'object'
    ? body.promptOverrides as Record<string, unknown>
    : undefined,
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create shared rubric/context validation contract with tests</name>
  <files>supabase/functions/_shared/rubric-context.ts, tests/rubric-context-validation.test.ts</files>
  <behavior>
    - Test 1: Whitespace-only values fail as required input.
    - Test 2: Values above 4,000 characters fail with max-length guidance.
    - Test 3: Valid values within limit are trimmed and returned for persistence.
    - Test 4: Character counter helper returns `current` and `max` values for UI display.
  </behavior>
  <action>Create a runtime-neutral helper module for rubric/context validation in `supabase/functions/_shared/rubric-context.ts` with a 4,000-char max constant (locked decision). Keep this file dependency-free so both edge handlers and the Next.js app can import it. Add focused tests in `tests/rubric-context-validation.test.ts` that cover all behavior cases above.</action>
  <verify>
    <automated>yarn test -- tests/rubric-context-validation.test.ts</automated>
  </verify>
  <done>Validation helper exports and regression tests exist and pass for trim/empty/max-length behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce shared validation in projects POST/PATCH handlers</name>
  <files>supabase/functions/projects/index.ts</files>
  <behavior>
    - Test 1: Invalid rubric/context payloads map to 400-level validation errors.
    - Test 2: Valid rubric/context payloads use normalized (trimmed) values before persistence.
    - Test 3: PATCH/POST requests without rubric/context updates continue existing behavior.
  </behavior>
  <action>Integrate the shared validator into `handlePost` and `handlePatch` so `promptOverrides.analysis_system_prompt` is validated and normalized when present. Throw `ProjectValidationError` for invalid values and keep all non-rubric project update behavior unchanged. Do not add permission logic in this phase (Phase 2 scope).</action>
  <verify>
    <automated>yarn test -- tests/rubric-context-validation.test.ts</automated>
  </verify>
  <done>Projects edge handler rejects invalid rubric/context input and accepts normalized valid input through the same create/update API surface.</done>
</task>

</tasks>

<verification>
- `yarn test -- tests/rubric-context-validation.test.ts` passes.
- Invalid rubric/context payload handling maps to 400 responses via `ProjectValidationError`.
</verification>

<success_criteria>
- VAL-01 server-side validation contract exists and is exercised by automated tests.
- Projects edge POST/PATCH no longer accepts trimmed-empty or oversized rubric/context values.
</success_criteria>

<output>
After completion, create `.planning/phases/01-rubric-context-entry/01-rubric-context-entry-01-SUMMARY.md`
</output>
